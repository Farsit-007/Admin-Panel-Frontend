import CreateSkill from "@/components/modules/skill/skillForm";

const page = () => {
    return (
        <div className="flex items-center justify-center">
            <CreateSkill/>
        </div>
    );
};

export default page;